```php
# 绑定原理有3种
# 绑定别名，也就相当于没绑一样，最后仍然是直接去make解析了
# 直接绑在alias

# bind绑定
# 给你套上一层匿名函数，然后实例化时使用
# 直接绑在bindings上


# instance绑定
# 直接绑在instance数组上


# 上下文绑定
# 绑定在上下文的数组上，等待需要解析你的时候，查看上下文数组，是否满足了条件，如果满足条件直接使用你要绑定的匿名函数或字符串


# extend绑定
# 相当于解析之后，调用你给的扩展数组，然后返回你本身
```

