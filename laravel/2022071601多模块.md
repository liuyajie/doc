```php
# 无论开发什么包，扩展包或多模块包
# 1、在composer.json中写明自己要注入的服务，也就是框架启动时需要把我的内容加载上去
# 之前是手动添加，现在都是自动添加上

# 2、框架启动时，首先加载你的服务，注册服务或服务别名
# 以多模块来说，相当于改变路由文件、注册配置文件等等

# 3、执行服务提供者的boot方法
# 如果是命令行的话，如果是vendor：publish的话，那就看boot方法里边是否有往发布里边注册，如果有的话， 则最后执行你
# 相当于拿到所有注册的信息，等待用户选择，让后我们执行
```

